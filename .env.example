# Set the purchased canonical domain before production. Unconfigured builds are noindex.
SITE_URL=
# Optional during local design work. Both required for live CMS content.
SANITY_PROJECT_ID=
SANITY_DATASET=
# Optional read-only token for a private dataset, used at build time only.
SANITY_READ_TOKEN=
